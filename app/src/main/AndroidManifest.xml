<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasakhirgan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TugasAkhirGan">
        <activity android:name=".riwayat" />
        <activity android:name=".ImageUpload" />
        <activity android:name=".activity_pembayaran" />
        <activity android:name=".activity_instalasiserver" />
        <activity android:name=".activity_perdaganganti" />
        <activity android:name=".activity_logout" />
        <activity android:name=".Activity_konfigurasijaringan" />
        <activity android:name=".activity_jasakonsultan" />
        <activity android:name=".UserActivity" />
        <activity android:name=".Register" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>